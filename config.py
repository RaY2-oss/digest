# -*- coding: utf-8 -*-
"""
config.py — центральный файл конфигурации проекта дайджеста.

ЗДЕСЬ хранятся:
  - фильтры сбора статей из GDELT GKG (QUERIES_GKG), используются daily_collector.py;
  - пути к БД, логам, выходным файлам;
  - прокси Xray (socks5h);
  - секреты OpenRouter и Telegram (заполняются вручную ниже).

ВАЖНО про socks5h: суффикс "h" означает, что DNS-резолвинг
выполняется на стороне прокси (Xray), а не локально. Это обязательно
для корректного обхода блокировок — иначе домены резолвятся на сервере
и трафик частично идёт мимо прокси.
"""

import os


def _load_dotenv(path: str) -> None:
    """Минимальный парсер .env без внешних зависимостей: строки вида KEY=VALUE.
    Уже заданные переменные окружения не перетираются (os.environ.setdefault),
    поэтому значение можно переопределить через окружение процесса/systemd."""
    try:
        with open(path, "r", encoding="utf-8") as fh:
            for line in fh:
                line = line.strip()
                if not line or line.startswith("#") or "=" not in line:
                    continue
                key, _, val = line.partition("=")
                os.environ.setdefault(key.strip(), val.strip().strip('"').strip("'"))
    except FileNotFoundError:
        pass


_load_dotenv(os.path.join(os.path.dirname(os.path.abspath(__file__)), ".env"))

# ---------------------------------------------------------------------------
# Прокси Xray (SOCKS5 с удалённым DNS). Отдельный inbound порт 10800.
# Порт 10443 — VLESS/REALITY backend nginx, SOCKS5 не поддерживает.
# ---------------------------------------------------------------------------
PROXIES = None  # прямой доступ, WARP недоступен из AlexHost

# ---------------------------------------------------------------------------
# Пути проекта. Вычисляются от расположения этого файла, поэтому остаются
# абсолютными (нужно для cron), но не привязаны к /opt/digest — проект можно
# развернуть в любом каталоге.
# ---------------------------------------------------------------------------
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
# DIGEST_DB_PATH позволяет собрать базу заново в отдельный файл и
# подменить ею боевую только после успешного прогона.
DB_PATH = os.environ.get("DIGEST_DB_PATH") or os.path.join(
    BASE_DIR, "data", "digest.db")
OUTPUT_DIR = os.path.join(BASE_DIR, "output")
LOG_DIR = os.path.join(BASE_DIR, "logs")

# ---------------------------------------------------------------------------
# Модель эмбеддингов (скачается автоматически в ~/.cache/huggingface/ при
# первом запуске, ~90 МБ). Размерность вектора = 384 (float32).
# ---------------------------------------------------------------------------
EMBEDDING_MODEL = "intfloat/multilingual-e5-small"
EMBEDDING_DIM = 384

# Порог отсева слишком коротких статей (символов).
MIN_TEXT_LENGTH = 300

# Сколько страниц за прогон дозволено переоткрыть браузером (0 — выключить).
# camoufox — целый Firefox: ~15 секунд на страницу, поэтому он не участвует в
# общей загрузке и идёт последней ступенью спасения — после архива, только по
# тем адресам, которых в архиве не нашлось (см. daily_collector.rescue_from_
# archive и шапку archive_fetch). Замер на сотне таких адресов: requests 23,
# curl_cffi 23, camoufox 32, архив 39, вся лестница вместе 62 из 100. Потолок
# взят по факту: "short" набирается около 60 в сутки на четыре прогона, то
# есть 20 покрывают обычный прогон целиком и держат добавку под пять минут.
CAMOUFOX_MAX = 20

# Итоговый размер дайджеста: sunday_processor_mmr.py отбирает эту сумму
# статей по квотам QUOTA = {"TR": 7, "CA": 7, "SC": 6} (+ добор из MIX при дефиците).
N_CLUSTERS = 20

# ---------------------------------------------------------------------------
# СЕКРЕТЫ — ЗАПОЛНИТЬ ВРУЧНУЮ.
# ---------------------------------------------------------------------------
# 1) OPENROUTER_API_KEY  — задаётся в /opt/digest/.env (не в этом файле).
#    Как получить:
#      - Зайти на https://openrouter.ai/ , нажать Sign In (можно через Google).
#      - Открыть https://openrouter.ai/keys , нажать "Create Key".
#      - Скопировать ключ вида "sk-or-v1-..." в .env строкой OPENROUTER_API_KEY=...
#        Бесплатные модели (:free) доступны без пополнения баланса (суточные лимиты).
OPENROUTER_API_KEY = os.environ.get("OPENROUTER_API_KEY", "")

# 1b) GROQ_API_KEY — платный фолбэк, когда суточный лимит :free-моделей
#     OpenRouter исчерпан (429 free-models-per-day). Ключ вида "gsk_..." с
#     https://console.groq.com/keys , в .env строкой GROQ_API_KEY=...
#     Пусто -> фолбэк выключен, поведение как раньше (вернём None).
GROQ_API_KEY = os.environ.get("GROQ_API_KEY", "")

# 1c) GOOGLE_API_KEY — следующий фолбэк после Groq (Google AI Studio / Gemini).
#     Ключ с https://aistudio.google.com/apikey , в .env строкой GOOGLE_API_KEY=...
#     Пусто -> этот фолбэк выключен.
GOOGLE_API_KEY = os.environ.get("GOOGLE_API_KEY", "")

# 2) TELEGRAM_BOT_TOKEN  — задаётся в /opt/digest/.env (не в этом файле).
#    Как создать бота:
#      - В Telegram открыть чат с @BotFather.
#      - Команда /newbot -> задать имя (например "IV RAN Digest")
#        и username (должен заканчиваться на "bot", напр. iv_ran_digest_bot).
#      - BotFather пришлёт токен вида "123456789:AAExxxxxxxxxxxxxxxxxxxxxxxx".
#        Скопировать его в .env строкой TELEGRAM_BOT_TOKEN=...
TELEGRAM_BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN", "")

# 3) TELEGRAM_CHAT_ID — задаётся в /opt/digest/.env (не в этом файле).
#    Как получить chat_id личного чата / группы / канала:
#      Вариант А (личный чат):
#        - Написать боту любое сообщение.
#        - Открыть в браузере (можно через прокси):
#          https://api.telegram.org/bot<ТОКЕН>/getUpdates
#        - Найти "chat":{"id": 123456789 ...} — это chat_id (для себя это
#          положительное число).
#      Вариант Б (группа):
#        - Добавить бота в группу, отправить сообщение.
#        - В getUpdates chat_id группы будет ОТРИЦАТЕЛЬНЫМ (напр. -1001234567890).
#      Вариант В (канал):
#        - Сделать бота администратором канала.
#        - Опубликовать пост, посмотреть getUpdates -> "channel_post" -> chat_id
#          (для каналов формата -100...).
TELEGRAM_CHAT_ID = os.environ.get("TELEGRAM_CHAT_ID", "")

# ---------------------------------------------------------------------------
# Секреты не должны попадать в логи. Исключения requests стрингуют полный URL
# вместе с токеном (".../bot<ТОКЕН>/getUpdates"), поэтому обычный
# log.error("...: %s", exc) утекал токен в bot_listener.log и в syslog.
# Чистить каждый вызов log.* по отдельности бессмысленно — подменяем фабрику
# записей: она глобальная, так что редактируется КАЖДАЯ запись любого логгера
# в процессе, включая те, что появятся позже.
# ---------------------------------------------------------------------------
_SECRETS = [OPENROUTER_API_KEY, GROQ_API_KEY, GOOGLE_API_KEY, TELEGRAM_BOT_TOKEN]


def _install_secret_redaction() -> None:
    import logging

    secrets = [s for s in _SECRETS if s and len(s) >= 8]
    if not secrets:
        return
    original_factory = logging.getLogRecordFactory()

    def factory(*args, **kwargs):
        record = original_factory(*args, **kwargs)
        try:
            message = record.getMessage()
        except Exception:
            return record
        cleaned = message
        for secret in secrets:
            cleaned = cleaned.replace(secret, "***REDACTED***")
        if cleaned != message:
            record.msg, record.args = cleaned, ()
        return record

    logging.setLogRecordFactory(factory)


_install_secret_redaction()

# ---------------------------------------------------------------------------
# Фильтры сбора статей из GDELT GKG (15-минутные дампы). Индекс в списке =
# query_index в БД. daily_collector.py прогоняет каждый фильтр по themes
# (GDELT GKG theme-коды) и locations (двухбуквенные FIPS-коды стран:
# TU=Турция, KZ=Казахстан, UZ=Узбекистан, TX=Туркменистан, KG=Киргизия,
# TI=Таджикистан, GG=Грузия, AM=Армения, AJ=Азербайджан).
# ---------------------------------------------------------------------------
QUERIES_GKG = [
    {   # Запрос 0 — наука, образование, молодёжь/студенты в Турции, ЦА и ЮК
        #
        # ВАЖНО: раньше здесь были ещё TAX_FNCACT_STUDENT/TEACHER/PROFESSOR/
        # RESEARCHER/CHILD/CHILDREN. Это НЕ тематические теги, а теги типа
        # актора у GDELT — они срабатывают на любое упоминание "учителя"
        # или "студента" в тексте (например, жертва в криминальной хронике),
        # никак не проверяя, что статья ДЕЙСТВИТЕЛЬНО о науке/образовании.
        # Из-за этого в БД попадала масса нерелевантного мусора ещё на этапе
        # дешёвого пред-фильтра, до полноценной LLM-проверки. Убраны —
        # реальную релевантность отсеивает LLM-фильтр (см. daily_collector.py).
        #
        # По той же причине убраны SOC_POINTSOFINTEREST_SCHOOL/UNIVERSITY/
        # COLLEGE: это теги УПОМИНАНИЯ объекта, а не темы статьи — школа как
        # место происшествия размечается ими так же, как школа как предмет
        # реформы. Замер 30.07.2026: ни для одной из 607 строк недели такой тег
        # не был единственной причиной прохода, то есть терять нечего.
        "themes": [
            "EDUCATION",
            "WB_470_EDUCATION",
            "SCIENCE",
        ],
        "locations": ["TU", "KZ", "UZ", "TX", "KG", "TI", "GG", "AM", "AJ"],
        # Тема и страна должны быть названы в пределах одного абзаца — это и
        # есть та связь, которой в GKG нет напрямую (см. gkg_filter.py).
        # Оба порога логируются каждым прогоном в виде перцентилей, поэтому
        # подбираются по наблюдаемым распределениям, а не на глаз.
        "max_theme_loc_gap": 400,
        "min_country_share": 0.30,
    },
    {   # Запрос 1 — действия внешних акторов в ЦА и ЮК
        "themes": [
            "EDUCATION",
            "WB_470_EDUCATION",
            "SCIENCE",
            "WB_831_GOVERNANCE",
        ],
        "locations": ["KZ", "UZ", "TX", "KG", "TI", "GG", "AM", "AJ"],
        # Тема и страна должны быть названы в пределах одного абзаца — это и
        # есть та связь, которой в GKG нет напрямую (см. gkg_filter.py).
        # Оба порога логируются каждым прогоном в виде перцентилей, поэтому
        # подбираются по наблюдаемым распределениям, а не на глаз.
        "max_theme_loc_gap": 400,
        "min_country_share": 0.30,
    },
]



# ---------------------------------------------------------------------------
# Отсев кандидатов ДО обращения к LLM
# ---------------------------------------------------------------------------
# Порог косинуса, выше которого две статьи считаются одним сюжетом.
# Важно: все члены кластера всё равно пишутся в БД. LexRank в
# sunday_processor_mmr выражает важность через плотность графа схожестей —
# тридцать изданий, перепечатавших один сюжет, образуют плотную клику, и
# именно она даёт высокий скор. Удаление дублей обнулило бы этот сигнал.
# Экономятся только LLM-запросы: судится один представитель кластера.
# Порог склейки перепечаток. Замер 11.08.2026 на 7806 размеченных парах
# (кластеры story_overview: внутри ключа — подтверждённые перепечатки одного
# события, между ключами — разные события), модель та же e5-small:
#
#   порог   поймано перепечаток   ложных склеек
#   0.95           16.6%              0.0%      <- стояло здесь
#   0.90           90.3%              0.2%
#   0.878          98.7%              1.5%      <- максимум точности
#
# 0.95 стоял ВЫШЕ типичной похожести перепечаток (средний косинус внутри
# сюжета 0.929), поэтому пропускал пять дубликатов из шести: LLM судила одно
# событие по нескольку раз и жгла суточную квоту.
#
# Взят 0.90, а не оптимальный по точности 0.878: ошибки тут несимметричны.
# Ложная склейка выбрасывает статью из рассмотрения совсем, пропущенный
# дубликат стоит лишь одного лишнего вызова. При 0.90 ловится девять
# перепечаток из десяти ценой двух ложных склеек на тысячу.
#
# Порог привязан к МОДЕЛИ: у каждой своё распределение косинусов (у bge-m3
# среднее внутри сюжета 0.776 против 0.929 у e5). Меняешь модель — меряй
# заново, переносить это число вслепую нельзя.
DEDUP_COSINE = 0.90

# Сколько батчей уходит в LLM параллельно.
LLM_PARALLEL_BATCHES = 5

# ---------------------------------------------------------------------------
# Политический вес (entities.py) — второй фактор важности рядом с LexRank
# ---------------------------------------------------------------------------
# LexRank меряет только плотность связей в графе схожестей, поэтому пачка
# однотипных локальных заметок («сельский школьник сдал экзамен на максимум»)
# образует такую же клику, как настоящий общенациональный сюжет. Второй фактор
# смотрит на СУБЪЕКТОВ статьи (персоны/организации из GKG V1Persons/
# V1Organizations): заметный субъект = встречается минимум в ENTITY_MIN_DF
# статьях недельного корпуса. У локальной заметки такой найдётся один
# (профильное министерство), у национального сюжета — 3-5 разных.
ENTITY_MIN_DF = 3      # порог заметности субъекта (док-частота по неделе)
ENTITY_FULL_AT = 4     # столько заметных субъектов = полный вес фактора

# ---------------------------------------------------------------------------
# Сюжеты и веса важности (sunday_processor_mmr._importance).
#
# Раньше в отбор шла каждая перепечатка отдельной записью. Из-за этого
# терялся сильнейший бесплатный признак масштаба — СКОЛЬКО РЕДАКЦИЙ вообще
# написали о событии, — а MMR мог отдать половину квоты зеркалам одной ленты.
# Теперь почти-дубликаты схлопываются в сюжет (STORY_COSINE), и важность
# считается по пяти факторам:
#
#   scale    — единственный фактор, который СМОТРИТ В СОБЫТИЕ: судья-LLM,
#              уже читавший статью при отборе, той же строкой отвечает, что
#              это — закон, программа, соглашение, новый университет (3),
#              заметная перемена в одном учреждении (2) или разовый местный
#              шаг (1). Остальные четыре меряют текст и корпус вокруг него,
#              поэтому рутина крупного ведомства обходила по ним реформу,
#              о которой написали два издания. Вес самый большой намеренно:
#              шкала грубая, связки внутри неё разбирают охват и topic.
#              Выключается сам, пока в окне нет ни одной оценки;
#   lexrank  — тематическая центральность в недельном окне;
#   coverage — охват: сколько РАЗНЫХ изданий написали о сюжете (importance.
#              publisher: сети-перепечатки считаются за одно). Замер по неделе
#              27.07: национальные сюжеты (закон о студенческой амнистии,
#              путеводитель по YKS, лунная программа) — 13-29 изданий,
#              локальная заметка про школьника — одно;
#   entity   — политический вес субъектов (см. блок выше);
#   topic    — вероятность локального предфильтра (prefilter.score): та же
#              дистиллированная модель, что режет мусор до LLM, здесь работает
#              непрерывной мерой «насколько это вообще наша тема». Без неё в
#              тонких корзинах CA/SC наверх лезли шахматы, партстроительство
#              и уголовная хроника — их пропустил судья-LLM, а охват только
#              усиливал. Фактор молча выключается, пока артефакта нет.
STORY_COSINE = 0.93          # косинус, при котором две статьи — один сюжет
COVERAGE_FULL_AT_MIN = 3     # нижняя граница шкалы охвата (см. _importance)
IMPORTANCE_W_LEXRANK = 0.0   # см. _importance: замер −0.117 против охвата
IMPORTANCE_W_COVERAGE = 0.25
IMPORTANCE_W_ENTITY = 0.15   # 0.0 — выключить фактор, не трогая код
IMPORTANCE_W_TOPIC = 0.20
IMPORTANCE_W_SCALE = 0.40

# Пересказ сюжета: в промпт уходят тексты нескольких его версий сразу.
# Разные редакции сохраняют разные детали (цифры, цитаты, имена), поэтому
# пересказ по нескольким версиям полнее, чем по одной. Потолок RETELL_CHARS_TOTAL
# держит промпт в пределах контекста бесплатных моделей — за ним начинаются
# галлюцинации и обрывы JSON.
#
# Раньше бюджет делился поровну и каждая версия резалась по первым N символов.
# Перепечатки начинаются одинаково, поэтому модель получала один и тот же лид
# четыре раза, а детали лежали ниже среза. Теперь версии разбираются на
# предложения и в промпт отбирается разное (retell_select), а потолки значат
# другое:
#
#   RETELL_SCAN_CHARS  — как глубоко смотрим В КАЖДУЮ статью. 6000 накрывает
#                        90-й процентиль длины статьи в корпусе (6894), а
#                        дальше у новостных сайтов идут «читайте также» и
#                        подвал. Замер 09.08.2026: 3000 стоил 13% фактов,
#                        10000 не добавил ничего (+0.4%);
#   RETELL_CHARS_TOTAL — сколько символов уходит в промпт. Не изменился, но
#                        занят теперь разными фактами: 329 разных слов на
#                        сюжет против 265 у обрезки, и промпт при этом короче;
#   RETELL_MAX_VERSIONS — сколько версий вообще разбираем. Шесть: замер даёт
#                        весь прирост к четырём-шести, восемь и двадцать
#                        добавляют меньше процента, а разбор стоит времени.
RETELL_MAX_VERSIONS = 6
RETELL_SCAN_CHARS = 6000
RETELL_CHARS_TOTAL = 6000

# Смысловая проверка пересказа отдельным вызовом (_validate_summary_meaning).
# Стражи формы ловят вид ошибки, а не смысл: «домашняя гибридная система тяги»
# вместо отечественной, «университеты, включая Анкару и Стамбул» вместо
# университетов этих городов, «согласно принятому TBMM» без существительного —
# всё это проходит любую regex-проверку и уходит читателю. Один вызов на сюжет,
# несколько десятков за недельный прогон. Выключатель нужен: проверка стоит
# запросов и может ошибаться сама.
# Выключено 14.08.2026: суточные квоты бесплатных провайдеров выбраны, судья
# получает 429, уходит в общую ротацию и добавляет к каждому сюжету ожидание.
# Включать, когда квота есть и закреплённая модель отвечает.
MEANING_CHECK = False

# Судья закрепляется за одной моделью, а не за пулом ":free". Замер 14.08.2026:
# три прогона на одном и том же входе дали три разных вердикта, потому что
# ротация каждый раз подставляла другую модель. Проверка, зависящая от лотереи,
# бесполезна: она не воспроизводится и её нельзя настроить. Пустая строка
# возвращает поведение к общей ротации.
MEANING_CHECK_MODEL = "gemini-3.7-flash"

# Журнал seen_urls: служебные строки без эмбеддинга (short/non_event/pending)
# старше стольких дней удаляются. Строки с эмбеддингом — обучающая выборка
# для предфильтра, они не трогаются.
SEEN_KEEP_DAYS = 30

# ---------------------------------------------------------------------------
# Локальный предфильтр (train_prefilter.py -> prefilter.py)
# ---------------------------------------------------------------------------
# Обучается на вердиктах самой LLM из seen_urls. Пороги допуска намеренно
# консервативные: чистая разметка начала копиться только с внедрением
# seen_urls, а прежние логи брать нельзя — отказ провайдеров молча помечал
# батч отклонённым (week_swap 21.07: 6006 кандидатов, 0 принятых).
PREFILTER_PATH = os.path.join(BASE_DIR, "data", "prefilter.joblib")
PREFILTER_MIN_LABELS = 3000      # меньше — тренер отказывается учиться
PREFILTER_MIN_MINORITY = 500     # минимальный размер класса меньшинства
PREFILTER_TARGET_RECALL = 0.97   # какую долю нужных статей обязан сохранить
PREFILTER_MIN_GAIN = 0.30        # режет меньше мусора — стадия не окупается
